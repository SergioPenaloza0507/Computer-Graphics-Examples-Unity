using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[RequireComponent(typeof(PointAndClickMovement))]
[RequireComponent(typeof(PointAndClickAnimationController))]
public class PointAndClickCharacter : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
