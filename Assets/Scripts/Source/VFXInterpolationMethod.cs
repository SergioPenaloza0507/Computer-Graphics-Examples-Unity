namespace VFX
{
    /// <summary>
    /// Definition for interpolation mode options for a VFX
    /// </summary>
    public enum VFXInterpolationMethod
    {
        StateMachine,
        Coroutine
    }
}
